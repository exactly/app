# exactly webapp

- inline as much as possible, avoid indirection, don't declare single use variables, functions, types, etc
- always omit optional arguments if default
- after finishing changes, always run `pnpm check` and `pnpm test`
